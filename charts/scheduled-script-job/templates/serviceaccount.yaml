{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "scheduledScriptJob.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
